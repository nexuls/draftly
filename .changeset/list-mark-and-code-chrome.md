---
"draftly": patch
---

Reveal a list marker's raw syntax only when the cursor is on the marker, rather than
anywhere on its line. Every bullet on a line used to un-style as soon as the caret entered
that line's text.
